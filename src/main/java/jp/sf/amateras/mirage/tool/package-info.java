/**
 * Provides tools to work with Mirage SQL. E.g. <code>EntityGenTask</code> will reverse engineer
 * a database and create Java entities (with the correct annotations) that maps to that schema.
 */
package jp.sf.amateras.mirage.tool;